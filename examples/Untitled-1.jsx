var oldContent = 'balblablalblabla',
    banList = ['a', 'abfa', 'baf'],
    newContent = '',
    contentList = oldContent.split('');

while(contentList.length){
  newContent += contentList.unshift();
  banList.some(item => {
    var len = item.length

  })
}
